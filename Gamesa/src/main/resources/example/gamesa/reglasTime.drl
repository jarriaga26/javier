package example.gamesa;

import java.util.Collection;

function void yourFunction(Collection<Object> colecion) {

	
}

rule "gamesaConTiempo"
	
	when
		Dispositivo( padre : padre == false )
		
	then
	    BeanTest $bean= new BeanTest();
	    $bean.metodo();
		System.out.println("si evalua 4.2:");
		Object $list = kcontext.getKieRuntime().getObjects();
		System.out.println("dd"+$list);
end